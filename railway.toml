[build]
builder = "NIXPACKS"
watchPatterns = ["backend/**", "requirements.txt", "run_server.py"]

[deploy]
startCommand = "python run_server.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
