[phases.setup]
nixPkgs = ["python310"]

[phases.install]
# Cache bust: 2024-12-11-v3-force-rebuild
cmds = ["pip install --upgrade pip", "pip install --no-cache-dir -r backend/requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
