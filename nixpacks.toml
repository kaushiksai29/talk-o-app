[phases.setup]
nixPkgs = ["python310"]

[phases.install]
# Cache bust: 2024-12-11-v2
cmds = ["pip install --upgrade pip", "pip install --no-cache-dir -r backend/requirements.txt"]

[start]
cmd = "python run_server.py"
